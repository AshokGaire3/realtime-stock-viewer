# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1

# Persistence (psycopg is the Postgres driver used in production; SQLite is stdlib)
sqlmodel==0.0.22
psycopg[binary]==3.2.3

# ML / forecasting
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1

# Historical price corpus for backtesting (server-side only). Note: versions
# below 1.x break against Yahoo's current auth and return empty frames.
yfinance==1.5.1

# AI tutor
anthropic==0.42.0

# Tests
pytest==8.3.4
pytest-asyncio==0.25.1

# NOTE: Prophet and torch (LSTM) are added in Phase 2/5. Prophet in particular
# has heavier build deps; the sklearn baseline works without them.
